#include <stdio.h>
#include <math.h>

int main(){
    printf("1+1 = %f", 1+1);
    printf("sin(30) = %f", sin(30));
    printf("sqrt(2*2 + 4*4) = %f", sqrt(2*2 + 4*4));
    printf("1+2*3 = %f", 1+2*3);
    return 0;
}
